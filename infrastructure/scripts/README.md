To run interop integration tests locally use the following commands.

```shell
./infrastructure/scripts/setup # sets up tooling; should be called at least once before `interop`
./infrastructure/scripts/cleanup # deletes build artifacts
./infrastructure/scripts/interop
```
