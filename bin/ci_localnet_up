#!/usr/bin/env bash

set -e

cd $ZKSYNC_HOME

cd docker/zk-environment-full
docker build --progress=plain -t zk-environment-full:latest -f docker/zk-environment-full/Dockerfile  .
cd ../..

mkdir -p ./volumes/postgres ./volumes/reth/data
docker-compose --profile runner up -d --wait
